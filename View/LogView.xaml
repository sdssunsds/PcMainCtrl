<Window x:Class="PcMainCtrl.View.LogView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PcMainCtrl.View"
        mc:Ignorable="d"
        Title="LogView" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="700"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <ScrollViewer Grid.Column="0">
            <TextBox HorizontalAlignment="Left" Margin="0,0,-0.4,0" TextWrapping="Wrap" Text="{Binding Log}" VerticalAlignment="Top" Height="420" Width="700"/>
        </ScrollViewer>
        <TextBox Grid.Column="1" HorizontalAlignment="Left" Margin="0,0,-0.4,0" TextWrapping="Wrap" Text="{Binding Other}" VerticalAlignment="Top" Height="420" Width="Auto"/>
    </Grid>
</Window>
